---
import { getEntry, render } from "astro:content";
import { SITE_TITLE } from "@/consts";
import Layout from "@/layouts/Layout.astro";
import { contentProseClass } from "@/lib/content-prose";
import { HomePresence } from "@/components/HomePresence";

const page = await getEntry("pages", "now");

if (!page) {
    throw new Error("Missing content page: now");
}

const { Content } = await render(page);
---

<Layout
    title={`${SITE_TITLE} — ${page.data.title}`}
    description={page.data.description}
>
    <div class={contentProseClass}>
        <HomePresence client:load />
        <Content />
    </div>
</Layout>
